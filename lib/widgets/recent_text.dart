import 'package:flutter/material.dart';

Text recentActivity() {
  return const Text(
    "Recent Activity",
    style: TextStyle(
        color: Colors.white, fontSize: 20, fontWeight: FontWeight.bold),
  );
}
